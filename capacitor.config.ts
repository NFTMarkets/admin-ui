import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'nft-rarity-ui',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
